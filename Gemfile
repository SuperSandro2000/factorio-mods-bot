# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# not all distributions like alpine have all default gems installed
gem 'bigdecimal', '~>1.0'
gem 'json'

gem 'httparty', '>= 0.18.0'
gem 'nokogiri', '>= 1.10.9'
